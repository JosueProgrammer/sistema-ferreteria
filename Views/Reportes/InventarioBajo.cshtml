@model List<Sistema_Ferreteria.Models.ViewModels.InventarioBajoVM>
@{
    ViewData["Title"] = "Inventario Bajo";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12 d-flex justify-content-between align-items-center">
            <h2 class="h3 mb-0 text-gray-800">Alertas de Inventario Bajo</h2>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-0">
                    <li class="breadcrumb-item"><a asp-action="Index">Dashboard</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Inventario Bajo</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="alert alert-warning border-0 shadow-sm rounded-4 d-flex align-items-center mb-4" role="alert">
        <i class="bi bi-exclamation-triangle-fill fs-4 me-3"></i>
        <div>
            Los siguientes productos tienen un stock actual igual o menor a su punto de reorden configurado. 
            <strong>Se recomienda realizar un pedido a proveedores pronto.</strong>
        </div>
    </div>

    <div class="card shadow-sm border-0 rounded-4">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th class="ps-4">Código</th>
                            <th>Producto</th>
                            <th>Categoría</th>
                            <th class="text-center">Stock Mínimo</th>
                            <th class="text-center">Stock Actual</th>
                            <th class="text-center pe-4">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4 font-monospace">@item.Codigo</td>
                                <td>
                                    <div class="fw-bold">@item.Nombre</div>
                                    <div class="x-small text-muted">ID: @item.IdProducto</div>
                                </td>
                                <td>
                                    <span class="badge bg-secondary-subtle text-secondary rounded-pill">@item.Categoria</span>
                                </td>
                                <td class="text-center">@item.StockMinimo.ToString("N0")</td>
                                <td class="text-center">
                                    @{
                                        var stockClass = item.StockActual <= 0 ? "text-danger fw-bold" : "text-warning fw-bold";
                                    }
                                    <span class="@stockClass">@item.StockActual.ToString("N2")</span>
                                </td>
                                <td class="text-center pe-4">
                                    <a asp-controller="Compras" asp-action="Index" class="btn btn-outline-primary btn-sm rounded-pill px-3">
                                        <i class="bi bi-cart-plus"></i> Comprar
                                    </a>
                                </td>
                            </tr>
                        }
                        @if (!Model.Any())
                        {
                            <tr>
                                <td colspan="6" class="text-center py-5 text-success">
                                    <i class="bi bi-check-circle fs-1 d-block mb-2"></i>
                                    Todo el inventario está en niveles óptimos.
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
